# 悬浮式手机底部导航设计

仅修改手机底部导航。保留首页 `/`、合集 `/authors`、我的 `/settings` 三个入口及原有页面跳转。

底栏使用完全不透明的黑白灰实色背景，悬浮于页面底部并适配 iPhone 安全区。三项等宽，采用线性图标加文字。当前项使用黑白灰实色圆角底块、颜色变化和一次短促上弹；按压时轻微缩小。禁止玻璃、半透明背景、`backdrop-filter` 和背景模糊。

首页精确匹配 `/`；合集匹配 `/authors` 及其子路径；我的匹配 `/settings` 及其子路径。再次点击当前项时阻止重复导航并平滑返回顶部。其他页面不错误选中任何入口。
